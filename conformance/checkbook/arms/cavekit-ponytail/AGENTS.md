# Arm instructions

Use the installed Cavekit `spec`, `build`, and `check` skills as one lightweight
process. `SPEC.md` is this arm's only specification. Apply Ponytail to every
implementation choice. Do not add another specification system.
