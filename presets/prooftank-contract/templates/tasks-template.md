## ProofTank completion tasks

- [ ] Apply the SimpleEnglish skill to technical instructions. Stop and record
      `unavailable` if this required skill is absent.
- [ ] Apply the Ponytail skill to implementation choices. Stop if this required
      skill is absent.
- [ ] Link every `REQ-*` and `INV-*` identifier to an implementation task and a
      deterministic check.
- [ ] Run the project-native tests and lint command. Record the exact revision
      and command.
- [ ] Report `passed`, `failed`, `stale`, `degraded`, `unknown`, and `waived`
      separately.
